#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int ogrencino,vize,final,not;
	printf ("ogrencino:");
	scanf("%d",&ogrencino );
	printf("vize:");
	scanf("%d",&vize);
	printf("final:");
	scanf("%d,&final");
	not=(vize*(0.4)+final*(0.6));
	printf("not:%d,not");
	
	
	return 0;
}
