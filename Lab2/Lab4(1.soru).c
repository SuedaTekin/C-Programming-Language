#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() { 
	int a,b,c;
	char *p=0;
	int *q=0;
	double *s=0;
	a=(p+3);
	b=(q+2);
	c=(s+1);
	printf("%d %d %d",a,b,c);
}
